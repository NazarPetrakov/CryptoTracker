<UserControl x:Class="CryptoTracker.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:components="clr-namespace:CryptoTracker.WPF.Components"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="Black"
             mc:Ignorable="d">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding InitializeCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>

        <TextBlock Margin="0,20,0,20"
                   HorizontalAlignment="Center"
                   FontSize="22"
                   Foreground="White"
                   Text="Top currencies " />

        <components:LoadingSpinner Grid.Row="1" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

        <ListView Grid.Row="1"
                  Background="Transparent"
                  ItemContainerStyle="{StaticResource ItemStyle}"
                  ItemsSource="{Binding Coins}"
                  SelectedItem="{Binding SelectedCoin}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDoubleClick">
                    <b:InvokeCommandAction Command="{Binding OpenDetailedCoinCommand}" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
            <ListView.Style>
                <Style TargetType="ListView">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource HeaderStyle}">
                    <GridViewColumn Width="150"
                                    CellTemplate="{StaticResource CoinNameTemplate}"
                                    Header="Coin" />

                    <GridViewColumn Width="60"
                                    DisplayMemberBinding="{Binding Symbol}"
                                    Header="Symbol" />

                    <GridViewColumn Width="100"
                                    DisplayMemberBinding="{Binding CurrentPrice, StringFormat='{}{0}$'}"
                                    Header="Price" />
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Row="2" Margin="10,10,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.7*" />
                    <ColumnDefinition Width="0.3*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="0,0,5,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Margin="0,0,5,3"
                               VerticalAlignment="Center"
                               FontSize="14"
                               Foreground="White"
                               Text="Search coin by symbol" />

                    <TextBox Grid.Column="1"
                             Height="30"
                             VerticalAlignment="Center"
                             VerticalContentAlignment="Center"
                             FontSize="14"
                             Text="{Binding SymbolToSearch, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCoinsCommand}" />
                        </TextBox.InputBindings>
                    </TextBox>
                </Grid>

                <StackPanel Grid.Column="1"
                            HorizontalAlignment="Left"
                            Orientation="Horizontal">
                    <Button Height="30"
                            Margin="0,0,5,0"
                            Padding="8,0"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Command="{Binding SearchCoinsCommand}"
                            Content="Search"
                            FontSize="14" />
                    <Button Height="30"
                            Margin="0,0,5,0"
                            Padding="8,0"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Command="{Binding ClearSearchCommand}"
                            Content="Clear"
                            FontSize="14" />
                </StackPanel>

            </Grid>

            <ComboBox Grid.Column="1"
                      Height="30"
                      VerticalContentAlignment="Center"
                      FontSize="14"
                      ItemsSource="{Binding Paginator.PageSizes}"
                      SelectedItem="{Binding Paginator.SelectedPageSize}"
                      Text="My Cities" />
        </Grid>

    </Grid>
</UserControl>
