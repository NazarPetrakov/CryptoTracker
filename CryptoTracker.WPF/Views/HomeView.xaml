<UserControl x:Class="CryptoTracker.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:components="clr-namespace:CryptoTracker.WPF.Components"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="Black"
             mc:Ignorable="d">

    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding InitializeCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Margin="0,20,0,20"
                   HorizontalAlignment="Center"
                   FontSize="22"
                   Foreground="White"
                   Text="Top currencies " />

        <components:LoadingSpinner Grid.Row="1" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

        <ListView Grid.Row="1"
                  MaxHeight="400"
                  Background="Transparent"
                  ItemContainerStyle="{StaticResource ItemStyle}"
                  ItemsSource="{Binding Coins}"
                  SelectedItem="{Binding SelectedCoin}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDoubleClick">
                    <b:InvokeCommandAction Command="{Binding OpenDetailedCoinCommand}" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
            <ListView.Style>
                <Style TargetType="ListView">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource HeaderStyle}">
                    <GridViewColumn Width="150"
                                    CellTemplate="{StaticResource CoinNameTemplate}"
                                    Header="Coin" />

                    <GridViewColumn Width="60"
                                    DisplayMemberBinding="{Binding Symbol}"
                                    Header="Symbol" />

                    <GridViewColumn Width="100"
                                    DisplayMemberBinding="{Binding CurrentPrice, StringFormat='{}{0}$'}"
                                    Header="Price" />
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
